---
title: 'Get List of Campaigns'
api: 'GET https://api.reply.io/v1/campaigns/list'
description: 'Get a list of all campaigns'
---

# Get List of Campaigns

Get a list of all campaigns in your account.

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API Key from settings
</ParamField>

## Response Body

<ResponseField name="campaigns" type="array">
  Array of campaign objects with the following properties:

<Expandable title="Campaign Object">
  <ResponseField name="id" type="integer">
    Campaign ID
  </ResponseField>

  <ResponseField name="name" type="string">
    Campaign name
  </ResponseField>

  <ResponseField name="created" type="string" format="date-time">
    Creation date and time
  </ResponseField>

  <ResponseField name="status" type="integer">
    Campaign status:
    - 0 = New
    - 2 = Active
    - 4 = Paused
  </ResponseField>

  <ResponseField name="emailAccounts" type="array">
    Email accounts used for the campaign
  </ResponseField>

  <ResponseField name="ownerEmail" type="string">
    Campaign owner's email
  </ResponseField>

  <ResponseField name="deliveriesCount" type="integer">
    Number of emails sent
  </ResponseField>

  <ResponseField name="opensCount" type="integer">
    Number of emails opened
  </ResponseField>

  <ResponseField name="repliesCount" type="integer">
    Number of replies received
  </ResponseField>

  <ResponseField name="bouncesCount" type="integer">
    Number of bounced emails
  </ResponseField>

  <ResponseField name="optOutsCount" type="integer">
    Number of opt-outs
  </ResponseField>

  <ResponseField name="outOfOfficeCount" type="integer">
    Number of out-of-office replies
  </ResponseField>

  <ResponseField name="peopleCount" type="integer">
    Total number of contacts
  </ResponseField>

  <ResponseField name="peopleFinished" type="integer">
    Number of contacts who finished
  </ResponseField>

  <ResponseField name="peopleActive" type="integer">
    Number of active contacts
  </ResponseField>

  <ResponseField name="peoplePaused" type="integer">
    Number of paused contacts
  </ResponseField>
</Expandable>
</ResponseField>

## Example Request

```bash
curl -X GET "https://api.reply.io/v1/campaigns/list" \
  -H "x-api-key: YOUR_API_KEY"
```

## Example Response

```json
{
    "campaigns": [
        {
            "id": 51755,
            "name": "My Campaign",
            "created": "2018-03-02T13:54:18.6766885+00:00",
            "status": 2,
            "emailAccounts": ["email@reply.io"],
            "ownerEmail": "owner@reply.io",
            "deliveriesCount": 5,
            "opensCount": 1,
            "repliesCount": 1,
            "bouncesCount": 0,
            "optOutsCount": 0,
            "outOfOfficeCount": 0,
            "peopleCount": 2,
            "peopleFinished": 0,
            "peopleActive": 2,
            "peoplePaused": 0
        }
    ]
}
```
